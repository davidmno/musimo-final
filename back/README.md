# API de musimo

Backend en Node.js y Express con MongoDB, JWT, bcrypt, Yup y acceso al catálogo de MusicBrainz.

```bash
cp .env.example .env
npm ci
npm run check
npm test
npm run dev
```

La documentación general y las variables de entorno están en el [`README.md`](../README.md) de la raíz.
